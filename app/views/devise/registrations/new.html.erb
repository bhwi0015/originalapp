<div class="home">
<%= form_with model: @user, url: user_registration_path, class: 'registration-main', local: true do |f| %>
    <%= link_to 'ログインはこちら', new_user_session_path, class: "log in btn" %>

  <div class='form-header'>
    <h1>HealthYourself</h1>
  </div>
  <div class='form-text-wrap'>
  <div class="form-group">
      <label class="form-text">ニックネーム</label>
    <%= f.text_area :nickname, class:"input-default", id:"nickname", maxlength:"40", autofocus: true %>
  </div>
  <div class="form-group">
      <label class="form-text">メールアドレス</label>
    <%= f.email_field :email, class:"input-default", id:"email", placeholder:"PC・携帯どちらでも可能" %>
  </div>
  <div class="form-group">
      <label class="form-text">パスワード</label>
    <%= f.password_field :password, class:"input-default", id:"password", placeholder:"6文字以上の半角英数字" %>
    <p class='info-text'>※英字と数字の両方を含めて設定してください</p>
  </div>
  <div class="form-group">
      <label class="form-text">パスワード(確認)</label>
    <%= f.password_field :password_confirmation, class:"input-default", id:"password-confirmation", placeholder:"同じパスワードを入力して下さい" %>

  <div class='register-btn'>
    <%= f.submit "登録" ,class:" btn register-red-btn" %>
   </div>
   </div>
<% end %>

</div>
